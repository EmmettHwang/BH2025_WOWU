# ============================================
# BH2025 WOWU - 바이오헬스 교육관리시스템
# Python 3.11+ | Python 3.14 호환
# ============================================

# ============================================
# Core Web Framework
# ============================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ============================================
# Database
# ============================================
pymysql>=1.1.0

# ============================================
# Environment & Configuration
# ============================================
python-dotenv>=1.0.0

# ============================================
# HTTP Clients
# ============================================
httpx>=0.25.0
requests>=2.31.0

# ============================================
# Data Processing
# ============================================
pandas>=2.1.0
numpy>=1.24.0

# ============================================
# PDF & Document Processing
# ============================================
reportlab>=4.0.0
pillow>=10.0.0

# ============================================
# AI & LLM Integration
# ============================================
openai>=1.3.0
google-generativeai>=0.3.0

# ============================================
# RAG (Retrieval-Augmented Generation)
# ============================================
# LangChain Core (최소 의존성)
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1

# Vector Store & Embeddings
faiss-cpu>=1.8.0
sentence-transformers>=2.3.1

# Document Loaders
pypdf2>=3.0.1
python-docx>=1.1.0

# Tokenization
tiktoken>=0.5.2

# ============================================
# Utilities
# ============================================
pydantic>=2.0.0

# ============================================
# 설치 명령
# ============================================
# pip install -r requirements.txt
#
# 선택적 설치 (개발환경):
# pip install pytest pytest-asyncio black flake8

# ============================================
# 변경 이력 (2024-12-31)
# ============================================
# ✅ 실제 사용 중인 패키지만 유지
# ❌ 제거된 패키지:
#    - langchain (langchain-core로 충분)
#    - langsmith (사용하지 않음)
#    - mysql-connector-python (pymysql만 사용)
#    - sqlalchemy (직접 pymysql 사용)
#    - fastapi-cors (FastAPI 내장 CORS 사용)
#    - aiohttp (httpx, requests로 충분)
#    - cryptography (사용하지 않음)
#    - pycryptodome (사용하지 않음)
#    - pyyaml (사용하지 않음)
#    - jsonpatch (사용하지 않음)
#    - dataclasses-json (pydantic으로 충분)
#    - tenacity (사용하지 않음)
#
# 📊 패키지 개수: 28개 → 20개 (28% 감소)
# 💾 설치 크기: ~2.5GB → ~1.8GB 추정 (약 700MB 절약)
#
# 🔍 검증 방법:
#    python check_imports.py
